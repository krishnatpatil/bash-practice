#!/usr/bin/env bash
#--------------------------------------------------------------------------------------------
# NAME:
#    tools/build.sh
#
# DESCRIPTION:
#    Build script which produces a RPM via Makefile.
#
# NEXT STEPS:
#    tools/deploy.sh

echo "--[CoDE]--[build.sh]------------------------------------------------------------------"
set -e

echo "--[CWD]-------------------------------------------------------------------------------"
pwd

echo "--[ls -ltra]----------------------------------------------------------------------------"
ls -ltra

echo "--[make all]---------------------------------------------------------------------------"
make

echo "--[ls -l *.rpm]-----------------------------------------------------------------------"
ls -l *.rpm
